speak_message:
    sequence:
      - service: media_player.volume_set
        data:
          entity_id: media_player.stuen
          volume_level: 20
      - service: tts.google_cloud_say
        entity_id: "all"
        data_template:
          message: "{{ message }}"
          cache: true
          language: 'da'